import { NewClientDialogData } from './new-client-dialog-data';

describe('NewClientDialogData', () => {
  it('should create an instance', () => {
    expect(new NewClientDialogData()).toBeTruthy();
  });
});
